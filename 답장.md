안녕하세요.
피드백 내용 반영해서 배포했으므로 확인 부탁드립니다. (https://formona.vercel.app/)

공유드릴 내용 및 문의 사항이 있으므로 확인 부탁드립니다.

① 수치 데이터에 대해서는 현재 데이터베이스를 설정하지 않은 상태이므로 임시로 json 파일로 다운로드해서 결과 데이터를 확인할 수 있도록 하고 있습니다. 추후 데이터베이스를 설정하게 되면 다른 방식(ex. 관리자 페이지에서 확인하거나 다운로드하는 방식)으로 데이터를 확인&추출할 수 있도록 생각하고 있습니다. 예시 데이터는 첨부 파일을 확인해주시고, 피드백 주시면 감사하겠습니다.

첨부 JSON 파일에는 아래와 같은 데이터가 포함됩니다.
- faceShape: 분석 결과에 포함되는 얼굴형 값입니다.
- selectedStyle: 사용자가 선택한 추천 눈썹 스타일의 id와 이름입니다.
- ipdMm: 사용자가 입력했거나 기본값으로 사용된 동공 간 거리(mm)입니다. 화면상의 픽셀 거리를 실제 mm 수치로 환산할 때 기준값으로 사용됩니다.
- pxToMmScale: 분석된 화면에서 1px이 몇 mm에 해당하는지 계산한 변환값입니다.
- pupilIpd: 카메라 화면에서 감지된 동공 간 거리 정보입니다. 홍채 기준인지, 눈 중심 기준인지와 감지 신뢰도가 포함됩니다.
- metrics: 실제로 확인해야 하는 주요 눈썹 측정 수치 7종입니다. 눈썹 시작점(SP), 최고점(HP), 끝점(EP), 전체 길이, 두께, 아치 높이, 좌우 눈썹 간격이 mm 단위로 저장됩니다.
- goldenRatioGuides: 좌우 눈썹의 SP/HP/EP 기준선과 황금비 기준점 좌표입니다. 좌표는 정규화 좌표, 픽셀 좌표, mm 환산 좌표로 함께 저장됩니다.
- overlayAnchors: AR 미리보기나 후속 가공에 사용할 좌우 눈썹의 핵심 기준점 좌표입니다.
- quality: 측정값의 전체 신뢰도, 예상 오차(mm), 실제 활용 가능 여부(reportable), 얼굴 정렬 상태가 포함됩니다.

참고로 현재 JSON에는 원본 얼굴 이미지/영상, 전체 FaceMesh 원본 landmark, 사용자 계정 정보, 결제 정보 등은 포함하지 않고 있으므로 추가 필요하면 반영하겠습니다.

측정 로직은 현재 아래 기준으로 반영되어 있습니다.
- SP(눈썹 시작점): 콧볼 중심을 기준으로 위쪽 눈썹 영역까지 수직선을 올려 시작점을 계산합니다.
- HP(눈썹 최고점): 홍채(iris) 중심을 기준으로 위쪽 눈썹 영역까지 수직선을 올려 최고점을 계산합니다. MediaPipe에서 홍채 좌표가 잡히지 않는 경우에는 눈 중심 좌표를 보조 기준으로 사용합니다.
- EP(눈썹 끝점): 인중 중심에서 눈꼬리 방향으로 이어지는 대각선을 기준으로 눈썹 끝점을 계산합니다.
- 눈썹 전체 길이: SP~EP 사이의 직선 거리를 계산합니다.
- 아치 높이: SP~EP 기준선에서 HP까지의 수직 거리를 계산합니다.
- 눈썹 간격: 좌우 SP 사이의 거리를 계산합니다.
- mm 환산: 사용자가 입력한 IPD(mm)와 카메라 화면에서 감지된 홍채 중심 간 픽셀 거리를 비교해서 px → mm 변환값을 만들고, 각 측정값을 mm 단위로 환산합니다.
- 좌우 평균: 좌측/우측 눈썹에서 각각 계산한 뒤, 최종 표시 수치는 평균값을 사용합니다.

추가로 현재는 SP~HP~EP 비율을 기반으로 황금비 기준점도 계산해서 JSON에 저장하고 있습니다. 다만 실제 HP 위치는 우선 홍채 중심 기준을 사용하고 있으며, 황금비 기준점으로 HP를 강제로 보정하지는 않고 있습니다.

이 측정 기준이 의도하신 방식과 맞는지 확인 부탁드립니다. 특히 HP를 홍채 중심 기준으로 둘지, 아니면 황금비 기준점을 더 적극적으로 반영해서 보정해야 할지 의견 주시면 그 기준에 맞춰 조정하겠습니다.


② AR 미리보기 관련하여 참고 이미지를 기반으로 수정하였으므로 확인 부탁드립니다.
