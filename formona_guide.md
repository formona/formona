# 🎨 Formona App — 디자인 가이드 (CI 기반)

> **문서 버전:** v1.0
> **작성일:** 2025년
> **작성자:** 포모나 COO
> **수신자:** 안정빈 개발자
> **참고 원본:** 주식회사 포모나 CI 매뉴얼 / 모나브로우 BI 매뉴얼

---

## 1. 브랜드 기본 정보

| 항목 | 내용 |
|---|---|
| 회사명 | 주식회사 포모나 (Formona) |
| 앱 제품명 | 모나브로우 (MONABROW) |
| 태그라인 | 첫 인상을 디자인하다, 눈썹 화장의 원픽. |
| 디자인 기준 | MONABROW BI 우선 적용 |

### 브랜드 스토리 (앱 톤앤매너 참고)

> 모나리자는 신비로운 미소에도 불구하고 눈썹이 없는 미완의 모습으로 알려져 있습니다.
> 포모나는 바로 이 상징적인 이야기에서 출발했습니다.
> 우리는 모든 '모나리자'들이 자신만의 눈썹을 찾아 진정한 걸작으로 완성되기를 바랍니다.

**앱 디자인 키워드:** 자연스러움 / 균형 / 자신감 / 정교함

---

## 2. 전용 색상 (Color)

### 메인 컬러

| 이름 | HEX | RGB | CMYK | Pantone | 용도 |
|---|---|---|---|---|---|
| 포모나브라운 | `#4F2C1D` | R79 G42 B27 | C62 M81 Y91 K48 | 4625C | 주요 텍스트, 버튼, 헤더 |
| 포모나골드 | `#727171` | R114 G113 B113 | C0 M0 Y0 K70 | — | 보조 UI, 아이콘 |

### 그라데이션

```
포모나 그라데이션 (브라운 계열)

위치 38%  →  C55 M65 Y90 K20
위치 50%  →  (중간값)
위치 85%  →  C70 M90 Y96 K68

⚠️ 정확한 HEX값은 안대표님께 원본 파일 확인 필요
```

### 배경 컬러 규정

```
✅ 기본 배경: White (#FFFFFF) — CI 규정상 필수
✅ 불가피한 경우: K20 / K40 / K60 / K80 / K100 그레이스케일만 허용
❌ 임의 색상 배경 사용 금지
```

---

## 3. 전용 서체 (Typography) 

### Pretendard (프리텐다드)

```
무료 서체
다운로드: https://cactus.tistory.com/306
```

### 적용 규정

| 항목 | 규정값 |
|---|---|
| 문자폭 | 100% (임의 변경 금지) |
| 자간 | 0px (임의 변경 금지) |
| 굵기 | Light / Bold 두 가지 사용 |

### 앱 내 굵기 가이드 (권장)

```
Bold   →  타이틀, 버튼 텍스트, 수치값
Light  →  본문, 설명, 안내 텍스트
```

---

## 4. 로고 사용 규정

### 심볼마크 (포모나 F 심볼)

```
✅ 원본 AI / SVG 파일 사용 필수
✅ 비례 유지하여 축소·확대만 허용
✅ 국문 가로형 / 영문 가로형 / 세로형 구분 사용
❌ 색상 임의 변경 금지
❌ 비율 / 자간 / 굵기 임의 변경 금지
❌ 변형, 회전, 기울임 금지
```

### MONABROW 로고

```
✅ BI 가로형 기준 적용
✅ 심볼 + 로고타입 조합형 사용
❌ 조합 비례 임의 변경 금지
```

### 최소 여백 규정

```
심볼마크 크기를 X라 할 때
가로형: 상하 0.15X / 좌우 0.3X 여백 확보
세로형: 상하 0.24X / 좌우 0.4X 여백 확보
```

---

## 5. 앱 적용 체크리스트

### 개발자 확인 사항

```
✅ Pretendard 폰트 앱에 임베드
✅ 포모나브라운 (#4F2C1D) 주요 UI에 적용
✅ 기본 배경 White 고정
✅ 로고는 원본 파일 받아서 적용 (직접 타이핑 금지)
✅ MONABROW BI 기준으로 스플래시 / 헤더 디자인
```

### 금지 사항

```
❌ 로고 이미지 늘리기 / 압축 금지
❌ 배경색 임의 변경 금지
❌ 자간 / 문자폭 변경 금지
❌ 브라운 외 임의 메인 컬러 사용 금지
```

---

## 6. 안대표님께 추가 요청 필요한 파일

| 파일 | 형식 | 용도 |
|---|---|---|
| 포모나 심볼마크 원본 | AI 또는 SVG | 앱 아이콘, 스플래시 | 
| MONABROW 로고 원본 | AI 또는 SVG | 헤더, 로딩 화면 | 
| 포모나 그라데이션 HEX값 | — | AR 오버레이, 배경 포인트 |
| 포모나골드 정확한 HEX | — | 보조 UI 컬러 확인 |

---

## 7. MONABROW MVP 프로토타입 허용 UI / 버튼 스타일 규칙

> 적용 범위: `./monabrow` Next.js MVP 프로토타입 전체  
> 기준: 현재 프로토타입 UI 감사 결과와 MONABROW BI 우선 원칙

### 7.1 UI 감사 요약

현재 프로토타입은 핵심 색상과 Pretendard는 적용되어 있으나, 일부 화면에서 BI 규정과 다른 시각 표현이 섞여 있다.

| 감사 항목 | 현재 발견된 경향 | MVP 적용 방향 |
|---|---|---|
| 배경 | glassmorphism, blur, radial accent 사용 | 기본은 흰색. 카메라 화면 외 장식 배경 금지 |
| 모서리 | `rounded-[40px]~[56px]`, pill 버튼 혼재 | 카드/패널 8~24px, 주요 CTA 28px 이하로 제한 |
| 자간 | `tracking-tight`, `tracking-tighter`, 과한 uppercase 자간 사용 | 본문/제목/버튼 자간 0px 고정 |
| 색상 | 흰색/브라운/회색 외 반투명 효과 다수 | `#FFFFFF`, `#4F2C1D`, `#727171`, K 그레이만 허용 |
| 버튼 | 화면별 높이, radius, shadow, active scale 값 불일치 | 허용된 버튼 타입만 사용 |
| 아이콘/일러스트 | 임시 아이콘과 이모지 사용 | lucide 아이콘 또는 단순 SVG 눈썹 프리뷰만 허용 |

### 7.2 허용 색상

MVP UI에서 사용할 수 있는 색상은 아래로 제한한다.

| 토큰 | HEX / 값 | 허용 용도 |
|---|---|---|
| `brandBrown` | `#4F2C1D` | 주요 텍스트, 주요 버튼 배경, AR 눈썹 윤곽선, 활성 상태 |
| `brandGray` | `#727171` | 보조 텍스트, 비활성 아이콘, 보조 버튼 테두리 |
| `white` | `#FFFFFF` | 앱 기본 배경, 패널/카드 배경 |
| `blackK20` | `rgba(0,0,0,0.2)` 또는 동등 K20 | 얇은 구분선, disabled border |
| `blackK40` | `rgba(0,0,0,0.4)` 또는 동등 K40 | disabled text, overlay caption |
| `successGuide` | `#2E7D32` | 얼굴 감지 성공 가이드 전용 |

금지:

- 임의 베이지/브라운 파생 배경색
- 보라/블루 계열 그라데이션
- 브랜드 HEX가 확정되지 않은 골드 포인트 컬러
- decorative radial gradient, orb, blur blob, bokeh 배경

### 7.3 허용 레이아웃과 표면

- 앱 기본 배경은 `#FFFFFF`로 고정한다.
- 모바일 1차 기준은 iPhone 12급 390px 너비와 Galaxy S21급 360px 너비이다.
- 콘텐츠 최대 너비는 기존 프로토타입처럼 430px까지 허용한다.
- 일반 정보 화면은 전체 화면 중앙 정렬보다 상단 로고/본문/하단 CTA 구조를 우선한다.
- 카드와 패널은 단일 레벨만 허용한다. 카드 안에 다시 장식 카드나 glass 카드 중첩을 만들지 않는다.
- 카드/패널 radius는 8px, 12px, 16px, 24px 중 하나만 사용한다.
- 카메라/AR 화면은 카메라가 주 화면이어야 하며, 안내/수치 패널은 하단 sheet 형태로만 겹친다.
- `backdrop-filter`, glassmorphism, 큰 그림자, floating blur surface는 MVP UI에서 사용하지 않는다.

### 7.4 허용 타이포그래피

- Pretendard만 사용한다.
- 문자폭은 100%, 자간은 0px이다.
- 허용 굵기는 Light, Bold 두 가지이다. Tailwind 기준 `font-light`, `font-bold`만 사용한다.
- 화면 제목은 24~32px, 본문은 14~16px, 보조 설명은 12~13px, 버튼 텍스트는 15~17px로 제한한다.
- 모바일 버튼 내부 텍스트가 줄바꿈되지 않도록 한국어 라벨은 짧게 쓴다.
- `tracking-tight`, `tracking-tighter`, 과한 `tracking-widest`는 사용하지 않는다. 로고 원본이 없는 임시 텍스트 로고에만 0px 자간의 Bold를 적용한다.

### 7.5 허용 버튼 타입

MVP에서는 아래 버튼 타입만 사용한다.

| 타입 | 용도 | 스타일 |
|---|---|---|
| Primary CTA | 다음, 시작하기, 카메라 허용, 선택 완료 | `#4F2C1D` 배경, 흰색 Bold 텍스트, 높이 52~56px, radius 28px 이하 |
| Secondary | 다시 찍기, 이전, 권한 재시도 | 흰색 배경, `#4F2C1D` 텍스트, `#4F2C1D` 1px 테두리, 높이 48~56px |
| Text Button | 측정 방법 보기, 브라우저 설정 안내 열기 | 배경 없음, `#727171` 또는 `#4F2C1D` 텍스트, underline 없이 44px 이상 터치 영역 |
| Icon Button | 닫기, 뒤로, 재시도, 정보 | lucide 아이콘 사용, 44x44px 이상, 흰색 또는 투명 배경 |
| Disabled | 유효하지 않은 IPD, 분석 전 다음 단계 | 기존 타입 유지, opacity 40%, pointer disabled, 추가 색상 사용 금지 |

버튼 공통 규칙:

- 최소 터치 영역은 44x44px이다.
- 버튼은 한 화면에서 Primary CTA 1개만 활성 상태로 둔다.
- 그림자는 사용하지 않거나 `rgba(79,44,29,0.12)` 이하의 매우 약한 shadow만 허용한다.
- `active:scale`은 0.98까지만 허용한다.
- 버튼 radius는 8px, 16px, 24px, 28px 중 하나만 사용한다.
- CTA 라벨은 실행 동작을 직접 말한다: `시작하기`, `다음`, `카메라 허용`, `선택 완료`, `다시 찍기`.
- `저장하기`는 MVP 제외 기능이므로 표시하지 않는다. 불가피한 데모 설명용이면 disabled + `v2 예정`으로만 표시한다.

### 7.6 허용 컴포넌트별 스타일

| 컴포넌트 | 허용 스타일 |
|---|---|
| Splash | 흰색 또는 `#4F2C1D` 단색 배경, MONABROW 임시 텍스트 로고, 1.5초 자동 전환 |
| IPD Input | 흰색 배경, 큰 숫자 입력, `mm` 단위 명확 표시, 정보 버튼은 Text Button |
| Permission | 카메라 권한 사유와 Primary CTA, 거부 시 브라우저별 설정 안내 |
| Camera Guide | 흰색 점선 타원, 감지 성공 시 `#2E7D32` 실선, 안내 문구는 짧게 |
| Face Shape Result | 얼굴형 1개, confidence/재촬영 안내, Primary CTA |
| Recommendation Card | 3개 수평 스크롤 카드, 1px 테두리, 선택 시 브라운 테두리/배경 강조 |
| AR Result | 카메라 위 brow outline, 하단 수치 sheet, `다시 찍기` Secondary |
| Modal | 흰색 배경, 24px 이하 radius, dim overlay는 K40 이하 |

### 7.7 금지 UI 패턴

- 로그인, 히스토리, 결제, 피부 분석, 눈썹 색상 시뮬레이션, 디바이스 스텐실 관련 CTA
- decorative glassmorphism 카드, 과한 blur, 중첩 카드
- 확정되지 않은 원본 로고를 임의로 변형한 로고형 UI
- 이모지를 기능 아이콘으로 사용하는 것
- 여러 개의 Primary CTA를 같은 화면에 배치하는 것
- 화면 내용을 가리는 고정 장식 요소
- 모바일 Safari/Chrome에서 안전 영역을 침범하는 하단 버튼

---

> 본 가이드는 주식회사 포모나 CI 매뉴얼 및 모나브로우 BI 매뉴얼을 기반으로 작성되었습니다.
> 디자인 적용 시 의문사항은 반드시 CI 관리 부서(포모나 COO)에 문의 후 검수를 받아야 합니다.
